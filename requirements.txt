# Core Dependencies
# These are required for the project to run
PyYAML

# AI API Dependencies
# Users can choose OpenAI-compatible or Google Gemini APIs.
openai>=2.45.0
google-genai>=2.11.0,<3

# Development Dependencies
# These are used for testing and development
pytest
pytest-mock
flake8

# Refactoring Dependencies
pydantic
pydantic-ai-slim[anthropic,google,openai,openrouter]==2.9.0
anthropic>=0.108.0,<1
json-repair
python-dotenv
requests
colorama
fastapi
uvicorn[standard]
python-multipart
# MCP adapter. Pin the stable SDK version exactly for this experimental branch;
# keep Starlette within the range used by the current FastAPI backend.
mcp==2.0.0
starlette<0.49

# Phonetic Matching Dependencies
pypinyin
pykakasi
jamo
python-Levenshtein

# Database
sqlmodel
aiosqlite
pytest-asyncio
